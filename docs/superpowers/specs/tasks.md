## Tasks

- [x] T001 Crear el scaffold del proyecto Android Kotlin (Gradle, módulo `app`, minSdk 29, Jetpack Compose habilitado) en `app/build.gradle.kts` y `settings.gradle.kts`
- [x] T002 [P] Configurar build nativo (CMake/NDK) para los targets JNI de llama.cpp y whisper.cpp en `app/CMakeLists.txt`, depends on T001
- [x] T003 [P] Agregar dependencia del SDK de openWakeWord (`openwakeword-android-kt`) y preparar el mecanismo para cargar el modelo custom de la palabra clave "Manuel" (`.onnx`, entrenado offline vía el pipeline de openWakeWord) en `app/build.gradle.kts` / `app/src/main/assets/wakeword/`, depends on T001 — **nota (2026-09-14): reemplaza la versión original de esta tarea, que agregaba el SDK de Porcupine + AccessKey de Picovoice; ver spec.md Clarifications y plan.md Research para el motivo del cambio de motor. Modelos genéricos (`melspectrogram.onnx`, `embedding_model.onnx`) ya vendorizados; falta entrenar y agregar `manuel.onnx` (tarea humana, ver comentario en build.gradle.kts).**
- [x] T004 [P] Definir el esquema del JSON de contenidos y redactar 4-6 lecciones de Matemática de nivel inicial/primario en `app/src/main/assets/content/matematica_lecciones.json`, depends on T001
- [x] T005 [P] Escribir tests unitarios de búsqueda de fragmentos (top 3-5 resultados relevantes vía FTS5) en `app/src/test/kotlin/com/manuel/mvp/rag/FragmentSearcherTest.kt`, depends on T004 — **nota (2026-09-14): el contrato de `FragmentSearcher` toma un `FragmentRowSource` (seam de ejecución SQL cruda) en vez de `SupportSQLiteDatabase` directamente, porque Robolectric no soporta FTS5; el test corre sobre `org.xerial:sqlite-jdbc`. T006 debe implementar `FragmentRowSource` contra `SupportSQLiteDatabase` en producción.**
- [x] T006 Implementar `ContentDatabase.kt`, `ContentDao.kt` (esquema SQLite/FTS5 versionado, migración inicial con downgrade probado) y `FragmentSearcher.kt` para satisfacer FR-006/FR-014 en `app/src/main/kotlin/com/manuel/mvp/rag/`, depends on T005 — **nota (2026-09-14): `FragmentSearcher.kt` debe usar el contrato `FragmentRowSource` definido en T005 (`app/src/test/kotlin/com/manuel/mvp/rag/FragmentSearcherTest.kt`); `ContentDao`/`ContentDatabase` implementan `FragmentRowSource` contra `SupportSQLiteDatabase`. Implementado vía `sdd` (feature `f_01m2gqh6bfh1tpv6nb7rerp53a`); verificación de tests real (`./gradlew test`) pendiente hasta contar con SDK de Android — se usó un harness JDBC sustituto, ver evidencia de la fase verify.**
- [x] T007 [P] Escribir tests unitarios de la memoria de sesión (ventana de 5 intercambios, borrado en "Dejar de escuchar" y por timeout de 5 min) en `app/src/test/kotlin/com/manuel/mvp/session/SessionMemoryTest.kt`, depends on T001 — **nota (2026-09-14): contrato `SessionMemory(clock, maxExchanges=5, inactivityTimeout=5min)`/`Exchange(question, answer)` con `record`/`currentExchanges`/`clear`, timeout evaluado de forma perezosa (`>=`, no `>`); implementado vía `sdd` (feature `f_01m2grw0jstezh36g779x8hm8a`).**
- [x] T008 Implementar `SessionMemory.kt` (ventana en memoria, sin persistencia) para satisfacer FR-010 en `app/src/main/kotlin/com/manuel/mvp/session/`, depends on T007 — **nota (2026-09-14): debe implementar exactamente el contrato ya probado por T007 (`app/src/test/kotlin/com/manuel/mvp/session/SessionMemoryTest.kt`) sin modificar ese archivo: `SessionMemory(clock, maxExchanges=5, inactivityTimeout=Duration.ofMinutes(5))`, `record`/`currentExchanges`/`clear`, timeout perezoso con semántica `>=`. Implementado vía `sdd` (feature `f_01m2gt4m0vscx94vn8y6v549s8`); las 6 pruebas de `SessionMemoryTest.kt` pasan (verificado con un harness kotlinc+JUnit standalone, sin SDK de Android disponible; corrida real de `./gradlew test` pendiente hasta contar con el SDK).**
- [x] T009 [P] Escribir tests unitarios del parseo de la palabra clave ("Manuel, <instrucción>": extracción cuando está presente, descarte silencioso cuando falta o no hay instrucción clara después) en `app/src/test/kotlin/com/manuel/mvp/audio/KeywordPrefixParserTest.kt`, depends on T001 — **nota (2026-09-14): contrato `KeywordPrefixParser.parse(transcript: String): String?`, keyword anclado al inicio (case-insensitive, coma opcional), `null` si falta la palabra clave o no hay instrucción clara después. Implementado vía `sdd` (feature `f_01m2gtrqhyy4z56ejh6cthy9td`); las 7 pruebas pasan contra un stub descartable (verificado con un harness kotlinc+JUnit standalone, sin SDK de Android disponible; corrida real de `./gradlew test` pendiente hasta contar con el SDK).**
- [x] T010 Implementar `WakeWordListener.kt` (wrapper de openWakeWord: arma/desarma con "Manuel", vuelve a escuchar en silencio si no hay instrucción clara) y el parseo de prefijo para satisfacer FR-002/FR-003/FR-004 en `app/src/main/kotlin/com/manuel/mvp/audio/`, depends on T003, T009 — **nota (2026-09-14): `KeywordPrefixParser.kt` implementa exactamente el contrato ya probado por T009 (`app/src/test/kotlin/com/manuel/mvp/audio/KeywordPrefixParserTest.kt`) sin modificar ese archivo. `WakeWordListener.kt` envuelve `WakeWordEngine` de `xyz.rementia:openwakeword:0.1.5` (un solo `WakeWordModel` "manuel", `DetectionMode.SINGLE_BEST`): `arm()`/`disarm()`, `armedDetections: Flow<WakeWordDetection>`, `resolveInstruction(String): String?` delegando a `KeywordPrefixParser.parse`. Implementado vía `sdd` (feature `f_01m2gv71sqk6tb77wwqzjctdzv`); las 7 pruebas de `KeywordPrefixParserTest.kt` pasan de verdad contra la implementación real (harness kotlinc+JUnit standalone, sin SDK de Android disponible); `WakeWordListener.kt` compila de verdad contra las clases reales de openwakeword/onnxruntime/kotlinx-coroutines (jar `android-all` de Robolectric en lugar de `android.jar`) -- corrida real de `./gradlew test`/instrumentada pendiente hasta contar con el SDK y el modelo `manuel.onnx` entrenado (ver nota de T003).**
- [x] T011 Implementar `AudioCaptureManager.kt` (ventana de captura de mic disparada tras la palabra clave) en `app/src/main/kotlin/com/manuel/mvp/audio/`, depends on T010 — **nota (2026-09-14): graba a 16kHz mono PCM16 (mismo formato que `AudioRecorder` de openWakeWord y que espera whisper.cpp) vía `android.media.AudioRecord` propio (no reusa el `AudioRecorder` interno de openWakeWord, privado a `WakeWordEngine`). La decisión de corte se delega a `CaptureWindowPolicy.kt` (máquina de estados pura: corta con audio tras silencio de cierre, corta en silencio si nunca hubo habla, límite máximo de duración) alimentada por `SpeechEnergyDetector.kt` (clasificador RMS puro por frame) — ambos con tests unitarios propios (`CaptureWindowPolicyTest.kt`, `SpeechEnergyDetectorTest.kt`, 10 casos en total, TDD dentro de esta misma tarea). Implementado vía `sdd` (feature `f_01m2gxq7xxv2xzpm6en4cx2qqq`); los 10 tests puros pasan de verdad (harness kotlinc+JUnit standalone); `AudioCaptureManager.kt` compila de verdad contra las clases reales de `android.media.*`/kotlinx-coroutines (jar `android-all` de Robolectric en lugar de `android.jar`) — corrida real de `./gradlew test`/instrumentada pendiente hasta contar con el SDK.**
- [x] T012 [P] Escribir tests unitarios de `WhisperTranscriber` (umbral de confianza, pedido de repetición cuando la confianza es baja) en `app/src/test/kotlin/com/manuel/mvp/stt/WhisperTranscriberTest.kt`, depends on T002 — **nota (2026-09-14): contrato `WhisperTranscriber(engine: WhisperEngine, confidenceThreshold: Float = 0.6f)` con `transcribe(audioPcm16: ShortArray): TranscriptionOutcome` (`Transcribed(text)` | `RepeatRequested`), umbral inclusivo (`>=`, mismo criterio que el timeout de `SessionMemory`). `WhisperEngine`/`TranscriptionResult` son el seam inyectable que separa la decisión de umbral del bridge JNI real a whisper.cpp (responsabilidad de T013); el test usa un `FakeWhisperEngine` hecho a mano. Implementado vía `sdd` (feature `f_01m2h02gz16d1ykbkxhcv0nams`); las 6 pruebas pasan contra un stub descartable (harness kotlinc+JUnit standalone, sin SDK de Android disponible; corrida real de `./gradlew test` pendiente hasta contar con el SDK).**
- [x] T013 Implementar `WhisperTranscriber.kt` (bridge JNI a whisper.cpp, modelo tiny/base) para satisfacer FR-005 en `app/src/main/kotlin/com/manuel/mvp/stt/`, depends on T012, T011 — **nota (2026-09-14): `WhisperTranscriber.kt` implementa exactamente el contrato ya probado por T012 (`app/src/test/kotlin/com/manuel/mvp/stt/WhisperTranscriberTest.kt`) sin modificar ese archivo. `NativeWhisperEngine.kt` implementa el seam `WhisperEngine` vía JNI (`app/src/main/cpp/whisper_jni.cpp`, agregado al target `manuel_native` en `app/CMakeLists.txt`): `whisper_init_from_file_with_params` (falla rápido si retorna NULL), `whisper_full` (idioma `"es"`, audio PCM16→float32 normalizado /32768), confianza = promedio de `whisper_full_get_token_p` sobre todos los tokens de todos los segmentos. Se hizo `git submodule update --init` de `third_party/whisper.cpp` (coincide con el commit ya fijado, sin drift) para revisar `whisper_jni.cpp` línea por línea contra la API real. Implementado vía `sdd` (feature `f_01m2h0pxffh3kcz05ws7785v21`); las 6 pruebas de `WhisperTranscriberTest.kt` pasan de verdad contra la implementación real (harness kotlinc+JUnit standalone). `whisper_jni.cpp` NO fue compilado (no hay toolchain NDK/CMake en este sandbox, misma limitación que T002) — build real y modelo whisper tiny/base pendientes hasta contar con un entorno Android completo.**
- [x] T014 [P] Escribir tests unitarios de `PromptBuilder` (turno actual + fragmentos RAG + memoria de sesión, prioridad del turno actual sobre el historial) en `app/src/test/kotlin/com/manuel/mvp/llm/PromptBuilderTest.kt`, depends on T006, T008 — **nota (2026-09-14): contrato `PromptBuilder().build(currentInstruction: String, ragFragments: List<ContentFragment>, sessionHistory: List<Exchange>): String` reutilizando `ContentFragment` (T006) y `Exchange` (T008) tal cual. La prioridad del turno actual (FR-007) se opera como posición: el turno actual (fragmentos + instrucción) debe quedar después del historial en el string armado — no hay forma de que `PromptBuilder` resuelva un conflicto semántico en sí mismo, eso es responsabilidad del LLM en tiempo de generación. `PromptBuilder.SYSTEM_INSTRUCTIONS` (constante no vacía con las reglas de FR-008) se fija por nombre/presencia, no por texto literal. Implementado vía `sdd` (feature `f_01m2h3m24tq6b1ghqy6exfwvvp`); las 7 pruebas pasan contra un stub descartable (harness kotlinc+JUnit standalone, sin SDK de Android disponible; corrida real de `./gradlew test` pendiente hasta contar con el SDK).**
- [x] T015 Implementar `PromptBuilder.kt` y `LlamaEngine.kt` (bridge JNI a llama.cpp, Llama 3.2 3B Instruct Q4_K_M) para satisfacer FR-007/FR-008 en `app/src/main/kotlin/com/manuel/mvp/llm/`, depends on T014, T002 — **nota (2026-09-14): `PromptBuilder.kt` implementa exactamente el contrato ya probado por T014 (`app/src/test/kotlin/com/manuel/mvp/llm/PromptBuilderTest.kt`) sin modificar ese archivo: orden historial→fragmentos→instrucción actual (prioridad del turno actual como posición, no como resolución de conflicto en tiempo real) y `SYSTEM_INSTRUCTIONS` (constante no vacía con las reglas de FR-008 más la regla de prioridad de FR-007 para el LLM). `LlamaEngine.kt` implementa el bridge JNI (`app/src/main/cpp/llama_jni.cpp`, agregado al target `manuel_native`): carga de modelo, tokenización, contexto+sampler greedy, loop de decodificación con tope de tokens y parada en EOG, `llama_model_free` en la ruta de fallo de init de contexto (sin leak). Se hizo `git submodule update --init` de `third_party/llama.cpp` (coincide con el commit ya fijado, sin drift) y se revisó `llama_jni.cpp` línea por línea contra `llama.h` y el ejemplo `examples/simple/simple.cpp` (sin dependencia de la librería `common`, deshabilitada por T002) — esta revisión encontró y corrigió un bug real (una constante `DEFAULT_MAX_TOKENS_FALLBACK` sin definir) antes de dejarlo como final. Implementado vía `sdd` (feature `f_01m2h3wmbj9sg9nextrq9v8n41`); las 7 pruebas de `PromptBuilderTest.kt` pasan de verdad contra la implementación real (harness kotlinc+JUnit standalone). `llama_jni.cpp` NO fue compilado (no hay toolchain NDK/CMake en este sandbox, misma limitación que T002/T013) — build real y modelo Llama 3.2 3B Instruct Q4_K_M pendientes hasta contar con un entorno Android completo.**
- [x] T016 Implementar `SpeechSynthesizer.kt` (wrapper de TextToSpeech, verificación de voz en español en el primer inicio) para satisfacer FR-009 en `app/src/main/kotlin/com/manuel/mvp/tts/`, depends on T001 — **nota (2026-09-14): `SpanishVoiceChecker.kt` (seam puro sobre `java.util.Locale`, testeable en JVM plana ya que `Locale` es un tipo del JDK) + `SpeechSynthesizer.kt` (wrapper de `TextToSpeech`, expone `hasSpanishVoice`/`speak`/`shutdown`). Implementado vía `sdd` (feature `f_01m2h7tn3sxwjkr3x98fz04nfb`); las 4 pruebas de `SpanishVoiceCheckerTest.kt` pasan de verdad; `SpeechSynthesizer.kt` compila de verdad contra las clases reales de `android.speech.tts.TextToSpeech` (jar `android-all` de Robolectric, sin SDK de Android disponible).**
- [x] T017 Implementar `LocalMetricsLogger.kt` (tiempos por etapa, errores, tasa de activación/falsos positivos de la palabra clave, sin audio ni texto de preguntas) para satisfacer FR-013 en `app/src/main/kotlin/com/manuel/mvp/metrics/`, depends on T001 — **nota (2026-09-14): `TurnMetrics`/`WakeWordActivationOutcome` no tienen ningún campo de audio/texto por diseño; ventana en memoria acotada (100 turnos por defecto, como `SessionMemory`), sin persistencia a disco. `POSSIBLE_FALSE_POSITIVE` es el proxy observable de la app para "posible falso positivo"; los falsos negativos (activación no detectada) no generan ningún evento y quedan fuera de alcance, a validar en T022. Implementado vía `sdd` (feature `f_01m2h82wjj3e09ghpb2rhm4s44`); las 6 pruebas pasan de verdad (JVM pura, sin dependencia de Android).**
- [x] T018 Implementar `ConversationPipeline.kt` orquestando palabra clave → STT → RAG → LLM → TTS con registro de métricas, para satisfacer FR-001/FR-011/FR-012 en `app/src/main/kotlin/com/manuel/mvp/pipeline/`, depends on T013, T015, T016, T017 — **nota (2026-09-14): `PipelineState` (Disarmed/Armed/Listening/Processing/Responding/Error) + `ConversationPipeline` inyectado con los 9 colaboradores + un `CoroutineScope`. Orden de validación por dependencia de datos: silencio (FR-004) → baja confianza (FR-005) → prefijo de palabra clave (FR-003) → turno completo (RAG+LLM+TTS+SessionMemory+métricas). Un error de turno se captura, se loguea con `TurnMetrics.error` y pasa a estado `Error`, sin desarmar ni limpiar la memoria de sesión. Sin test dedicado (todos los colaboradores salvo `WhisperTranscriber` necesitan hardware/motor nativo/voz TTS reales); verificado con una compilación combinada contra los 17 archivos reales de colaboradores. Implementado vía `sdd` (feature `f_01m2h8cy4sc5tmcqcggdnn00gk`).**
- [x] T019 [P] Escribir tests instrumentados de los estados de UI (armado, escuchando, procesando, respondiendo, error) y de accesibilidad (`contentDescription` en los botones) en `app/src/androidTest/kotlin/com/manuel/mvp/ui/MainScreenTest.kt`, depends on T018 — **nota (2026-09-14): contrato `MainScreen(state: AssistantState, onEscucharClick, onDejarDeEscucharClick, modifier)` stateless + `AssistantState` (Disarmed/Armed/Listening/Processing/Responding/Error), deliberadamente distinto de `PipelineState` (T018) para no acoplar `ui` a `pipeline`. Se agregaron dependencias `androidTestImplementation` de Compose test (`ui-test-junit4`, `androidx.test.ext:junit:1.3.0`) a `app/build.gradle.kts`. Sin compilación posible (los artefactos de test de Compose no están disponibles sin un sync real de Gradle, bloqueado por la falta de SDK de Android) — escrito y revisado línea por línea contra la API real y estable de `androidx.compose.ui.test`. Implementado vía `sdd` (feature `f_01m2h8sg21a3bmx0gmt2wsgav8`).**
- [ ] T020 Implementar `MainScreen.kt`, `AssistantState.kt` y `MainActivity.kt` (Compose, botones "Escuchar"/"Dejar de escuchar", estado visible) para satisfacer FR-001 en `app/src/main/kotlin/com/manuel/mvp/ui/` y `app/src/main/kotlin/com/manuel/mvp/MainActivity.kt`, depends on T019, T018
- [ ] T021 Configurar CI (GitHub Actions) corriendo unit tests y lint en cada PR antes de mergear a `main` en `.github/workflows/ci.yml`, depends on T001
- [ ] T022 Redactar el protocolo de prueba manual (30 preguntas + 5 diálogos multi-turno, sección 11 del spec fuente) para validar SC-001 a SC-011 en el hardware objetivo, en `docs/superpowers/specs/manuel-mvp-test-protocol.md`, depends on T020
