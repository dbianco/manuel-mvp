# Tasks: CI workflow (T021)

## Tasks

- [ ] T001 Write `.github/workflows/ci.yml` (pull_request + push triggers, submodule checkout, JDK 17, Android SDK setup, `testDebugUnitTest` + `lintDebug`, report upload, concurrency cancellation)
- [ ] T002 Validate the file as well-formed YAML matching the GitHub Actions workflow schema, depends on T001
